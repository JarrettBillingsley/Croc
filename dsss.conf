name = minid

[minid]
version(Posix) {
    buildflags += -version=MDReadline -llreadline -llhistory
}

[mdcl.d]
target = mdcl
buildflags = -release -O

[minidc.d]
target = minidc

[test.d]
target = mdtest
buildflags = -debug -g
